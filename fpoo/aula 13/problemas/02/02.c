#include <stdio.h>
#include <locale.h>

void posicao(){
	int total, i;
	char direcao[1][total];
	printf("número de comandos: ", total);
	scanf("%d", &total);
	for(i = 0; i <= total - 1; i++){
		printf("direçoes do comando: ");
		scanf("%s", &direcao[i]);
	}
	if()
	
}

int main(){
	setlocale(LC_ALL,"");
	posicao();
	return 0;
}
